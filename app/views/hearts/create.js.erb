var lovable_id = "#<%= dom_id(@heart.lovable) %>";
var lovable_class = ".<%= dom_class(@heart.lovable) %>";

$(lovable_id).find(lovable_class + "_actions").html("<%= j render "shared/actions", post: @heart.lovable %>");

