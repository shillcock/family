!!!
%html
  %head
    %meta{:charset => "utf-8"}/
    %meta{:content => "NOODP", :name => "ROBOTS"}/
    %meta{:content => "initial-scale=1", :name => "viewport"}/
    %title= content_for?(:title) ? yield(:title) : "The Shillcock Family Portal"
    = stylesheet_link_tag "application", media: "all"
    = csrf_meta_tags
  %body.page-body{class: page_class}
    %header
      = render "navigation"
    %main{role: "main"}
      = render "flashes"
      = yield
    = render "javascript"

